diff --git a/dev/lib/syntax.js b/dev/lib/syntax.js
index 71693081622ef30d536e339eb11147488b067fdd..f309375d1c8e8795e6a322ed8f52c3418853fede 100644
--- a/dev/lib/syntax.js
+++ b/dev/lib/syntax.js
@@ -36,6 +36,55 @@ export function gfmTable() {
   }
 }

+/**
+ * Treat a complete wiki link as opaque table-cell data.
+ *
+ * This construct runs before the table tokenizer interprets internal `|`
+ * characters as cell dividers. Failed `[[...]]` attempts roll back and retain
+ * ordinary GFM behavior.
+ *
+ * @type {import('micromark-util-types').Construct}
+ */
+const wikiLinkInTable = {partial: true, tokenize: tokenizeWikiLinkInTable}
+
+/**
+ * @this {TokenizeContext}
+ * @type {Tokenizer}
+ */
+function tokenizeWikiLinkInTable(effects, ok, nok) {
+  return start
+
+  /** @type {State} */
+  function start(code) {
+    if (code !== codes.leftSquareBracket) return nok(code)
+    effects.enter(types.data)
+    effects.consume(code)
+    return open
+  }
+
+  /** @type {State} */
+  function open(code) {
+    if (code !== codes.leftSquareBracket) return nok(code)
+    effects.consume(code)
+    return inside
+  }
+
+  /** @type {State} */
+  function inside(code) {
+    if (code === codes.eof || markdownLineEnding(code)) return nok(code)
+    effects.consume(code)
+    return code === codes.rightSquareBracket ? close : inside
+  }
+
+  /** @type {State} */
+  function close(code) {
+    if (code !== codes.rightSquareBracket) return nok(code)
+    effects.consume(code)
+    effects.exit(types.data)
+    return ok
+  }
+}
+
 /**
  * @this {TokenizeContext}
  * @type {Tokenizer}
@@ -205,6 +254,14 @@ function tokenizeTable(effects, ok, nok) {
       return headRowBreak
     }

+    if (code === codes.leftSquareBracket) {
+      return effects.attempt(
+        wikiLinkInTable,
+        headRowBreak,
+        headRowWikiLinkNok
+      )(code)
+    }
+
     // Anything else is cell data.
     effects.enter(types.data)
     return headRowData(code)
@@ -223,6 +280,15 @@ function tokenizeTable(effects, ok, nok) {
    * @type {State}
    */
   function headRowData(code) {
+    if (code === codes.leftSquareBracket) {
+      effects.exit(types.data)
+      return effects.attempt(
+        wikiLinkInTable,
+        headRowBreak,
+        headRowWikiLinkNok
+      )(code)
+    }
+
     if (
       code === codes.eof ||
       code === codes.verticalBar ||
@@ -236,6 +302,18 @@ function tokenizeTable(effects, ok, nok) {
     return code === codes.backslash ? headRowEscape : headRowData
   }

+  /**
+   * Continue ordinary table parsing when `[` does not start a complete wiki
+   * link.
+   *
+   * @type {State}
+   */
+  function headRowWikiLinkNok(code) {
+    effects.enter(types.data)
+    effects.consume(code)
+    return headRowData
+  }
+
   /**
    * In table head row escape.
    *
@@ -567,6 +645,14 @@ function tokenizeTable(effects, ok, nok) {
       return factorySpace(effects, bodyRowBreak, types.whitespace)(code)
     }

+    if (code === codes.leftSquareBracket) {
+      return effects.attempt(
+        wikiLinkInTable,
+        bodyRowBreak,
+        bodyRowWikiLinkNok
+      )(code)
+    }
+
     // Anything else is cell content.
     effects.enter(types.data)
     return bodyRowData(code)
@@ -585,6 +671,15 @@ function tokenizeTable(effects, ok, nok) {
    * @type {State}
    */
   function bodyRowData(code) {
+    if (code === codes.leftSquareBracket) {
+      effects.exit(types.data)
+      return effects.attempt(
+        wikiLinkInTable,
+        bodyRowBreak,
+        bodyRowWikiLinkNok
+      )(code)
+    }
+
     if (
       code === codes.eof ||
       code === codes.verticalBar ||
@@ -598,6 +693,18 @@ function tokenizeTable(effects, ok, nok) {
     return code === codes.backslash ? bodyRowEscape : bodyRowData
   }

+  /**
+   * Continue ordinary table parsing when `[` does not start a complete wiki
+   * link.
+   *
+   * @type {State}
+   */
+  function bodyRowWikiLinkNok(code) {
+    effects.enter(types.data)
+    effects.consume(code)
+    return bodyRowData
+  }
+
   /**
    * In table body row escape.
    *
diff --git a/lib/syntax.js b/lib/syntax.js
index 00b45b02adb80dd2daf12a4a5dbf78ee80e0e83a..9f7e79dff13219938bcc094c61b266f7aac53c93 100644
--- a/lib/syntax.js
+++ b/lib/syntax.js
@@ -34,6 +34,58 @@ export function gfmTable() {
   };
 }

+/**
+ * Treat a complete wiki link as opaque table-cell data.
+ *
+ * This construct runs before the table tokenizer interprets internal `|`
+ * characters as cell dividers. Failed `[[...]]` attempts roll back and retain
+ * ordinary GFM behavior.
+ *
+ * @type {import('micromark-util-types').Construct}
+ */
+const wikiLinkInTable = {
+  partial: true,
+  tokenize: tokenizeWikiLinkInTable
+};
+
+/**
+ * @this {TokenizeContext}
+ * @type {Tokenizer}
+ */
+function tokenizeWikiLinkInTable(effects, ok, nok) {
+  return start;
+
+  /** @type {State} */
+  function start(code) {
+    if (code !== 91) return nok(code);
+    effects.enter("data");
+    effects.consume(code);
+    return open;
+  }
+
+  /** @type {State} */
+  function open(code) {
+    if (code !== 91) return nok(code);
+    effects.consume(code);
+    return inside;
+  }
+
+  /** @type {State} */
+  function inside(code) {
+    if (code === null || markdownLineEnding(code)) return nok(code);
+    effects.consume(code);
+    return code === 93 ? close : inside;
+  }
+
+  /** @type {State} */
+  function close(code) {
+    if (code !== 93) return nok(code);
+    effects.consume(code);
+    effects.exit("data");
+    return ok;
+  }
+}
+
 /**
  * @this {TokenizeContext}
  * @type {Tokenizer}
@@ -187,6 +239,9 @@ function tokenizeTable(effects, ok, nok) {
       seen = true;
       return headRowBreak;
     }
+    if (code === 91) {
+      return effects.attempt(wikiLinkInTable, headRowBreak, headRowWikiLinkNok)(code);
+    }

     // Anything else is cell data.
     effects.enter("data");
@@ -206,6 +261,10 @@ function tokenizeTable(effects, ok, nok) {
    * @type {State}
    */
   function headRowData(code) {
+    if (code === 91) {
+      effects.exit("data");
+      return effects.attempt(wikiLinkInTable, headRowBreak, headRowWikiLinkNok)(code);
+    }
     if (code === null || code === 124 || markdownLineEndingOrSpace(code)) {
       effects.exit("data");
       return headRowBreak(code);
@@ -214,6 +273,18 @@ function tokenizeTable(effects, ok, nok) {
     return code === 92 ? headRowEscape : headRowData;
   }

+  /**
+   * Continue ordinary table parsing when `[` does not start a complete wiki
+   * link.
+   *
+   * @type {State}
+   */
+  function headRowWikiLinkNok(code) {
+    effects.enter("data");
+    effects.consume(code);
+    return headRowData;
+  }
+
   /**
    * In table head row escape.
    *
@@ -513,6 +584,9 @@ function tokenizeTable(effects, ok, nok) {
     if (markdownSpace(code)) {
       return factorySpace(effects, bodyRowBreak, "whitespace")(code);
     }
+    if (code === 91) {
+      return effects.attempt(wikiLinkInTable, bodyRowBreak, bodyRowWikiLinkNok)(code);
+    }

     // Anything else is cell content.
     effects.enter("data");
@@ -532,6 +606,10 @@ function tokenizeTable(effects, ok, nok) {
    * @type {State}
    */
   function bodyRowData(code) {
+    if (code === 91) {
+      effects.exit("data");
+      return effects.attempt(wikiLinkInTable, bodyRowBreak, bodyRowWikiLinkNok)(code);
+    }
     if (code === null || code === 124 || markdownLineEndingOrSpace(code)) {
       effects.exit("data");
       return bodyRowBreak(code);
@@ -540,6 +618,18 @@ function tokenizeTable(effects, ok, nok) {
     return code === 92 ? bodyRowEscape : bodyRowData;
   }

+  /**
+   * Continue ordinary table parsing when `[` does not start a complete wiki
+   * link.
+   *
+   * @type {State}
+   */
+  function bodyRowWikiLinkNok(code) {
+    effects.enter("data");
+    effects.consume(code);
+    return bodyRowData;
+  }
+
   /**
    * In table body row escape.
    *
